name: resilient_client_api
description: Um wrapper HTTP em Dart focado em resiliência, retries e timeouts.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.11.0

# Add regular dependencies here.
dependencies:
  http: ^1.6.0
  meta: ^1.18.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
